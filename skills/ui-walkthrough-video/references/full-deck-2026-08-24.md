# Full Deck Build 2026-08-24 — 8:10 Master (15 slides)

## Trigger
User said "i want it full" after 15s style prototype. Required expanding from single-screen sample to 490s / 15-slide complete presentation.

## Batch Extraction (156 FULL of 175)
- SF Dental (Copy).pdf 78MB, 175 pages total; 156 FULL 390×844, 13 FRAG (<200px), 6 BOARD (>1000px). FRAG/BOARD ignored for video.
- Maps: P016 welcome_login, P023 role_select, P025 dentist_register, P039 pending_approval, P045 queue, P018 patient_home (good morning Maria), P019 my_appointments, P020 book, P087 submitted, P022 profile, P082 record, P021 messages, P028 requests, P027 calendar, P029 patients, P044 income, P075 transactions, P167 reports, P031 staff
- Code: `pymupdf.Matrix(3,3)` → 1170×2532 PNG to `frames/<p>_<name>.png` (135-220K each)

## TTS per slide
- Same voice `en-US-AriaNeural +12%` via `edge_tts.Communicate(text, voice, rate='+12%')` to `tts_full/NN_sid.mp3`
- Measure via `ffprobe -show_entries format=duration`; visual = `max(storyboard_base, tts+1.4)` (0.4 lead +1.0 tail)
- Total 490s (8:10) vs storyboard 7:57 drift — report ffprobe, not plan
- Script 15 slides verbatim (BERNADETTE owns 07 dashboard, 08 appointment, 09 records) — see DAR_Dental_Master_Presentation.html Deliverable 2

## Audio mix for long form
- BGM must exceed total: `ffmpeg -f lavfi -i sine=frequency=110:duration=492 -filter:a volume=0.04,lowpass=f=800` → `bgm_full.m4a` 492s then `with_volume_scaled(0.07)` vs narration 1.0 at `t_cursor+0.35` via `CompositeAudioClip([bgm] + tts_clips)`
- Short BGM pitfall scales linearly: 15.0 vs 15.2 → OSError at 15.02, for 490s need 492s

## Video assembly
- Not one giant make_frame (14700 frames hard to resume). Per-slide `VideoClip(make_frame, duration=vis)` @30fps then `CompositeVideoClip(clips, size=(1920,1080)).with_duration(total)`
- Title/cards/fan/workflow/pillars slides: static gradient + Ken Burns 1.0→1.015, cheap
- Phone slides: reuse spotlight engine (darken 55% rgba 77, hole blur 1, teal ring 180 + outer glow 70, inner white hairline, shadow blur 12, progress bar, pill label)
- Phone math: `factor=disp_w/1170`, `x0=960 - cx*3*factor`, `y0=540 - cy*3*factor`; rects in logical 390×844: notif(10,142,370,96), upcoming(10,265,370,125), quick_recent(10,400,370,400)
- Dashboard 07 uses multi-stage (FULL→upcoming→quick_recent→notif→return) vs other phones single spotlight

## Performance
- 456 frames (15.2s) = 80s @5 fps start; 14700 frames (490s) = 74 min @1.1 fps throttled on i5-11400H/RTX3050 4GB (thermal from 5→1.1)
- PIL LANCZOS+GaussianBlur is bottleneck `// ponytail: global PIL per-frame LANCZOS+blur, cache discrete scales if throughput matters`
- Don't promise "few minutes" for full deck; quote 40-80 min on i5/4GB
- File `build_full_presentation.py` 348→~550 lines, job proc_8bbe88e452fc background

## Deliverables (6 in one HTML master 70K)
- Deck table 15 rows, Script verbatim, Storyboard 00:00→07:57, Mapping PDF page→frame, Animation system 300ms cubic, Production steps (extract→Canva 1920×1080 for 01-04/14-15→Premiere→-16 LUFS→faststart)
- Master: `videos/DAR_Dental_Master_Presentation.html` printable; full video: `DAR-Dental-Clinic-Full-Presentation-7m57s.mp4` 1920×1080 30fps h264 491s 45MB

## Verification
- ffprobe 1920×1080 h264 30/1 490s, aac 44100 2ch; extract 1s/4s/7.5s/11s/14.5s thumbs vision_analyze upcoming = darkened+teal glow premium
- Keep Figma PDF as source of truth; HTML frames `design/frames/03_Patient_Home.html` used only to validate layout coords (29K)
